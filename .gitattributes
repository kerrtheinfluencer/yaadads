# Git hook scripts must keep LF line endings so their shebangs work on
# Git for Windows, macOS and Linux alike.
.githooks/* text eol=lf

# Generated by tools/build-changelog.js (writes LF) — keep the diff history clean.
CHANGELOG.md text eol=lf